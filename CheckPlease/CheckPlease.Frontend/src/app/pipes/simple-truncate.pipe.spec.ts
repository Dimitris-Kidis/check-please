import { SimpleTruncatePipe } from './simple-truncate.pipe';

describe('SimpleTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
